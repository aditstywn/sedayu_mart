import 'package:flutter/material.dart';

class ColorsApp {
  // Warna Hijau Utama (Pertanian)
  static const Color primary = Color(0xFF2E7D32); // Hijau tua alami
  static const Color lightGreen = Color(0xFF66BB6A); // Hijau muda
  static const Color darkGreen = Color(0xFF1B5E20); // Hijau sangat gelap
  static const Color accentGreen = Color(0xFF81C784); // Hijau aksen

  // Warna Background
  static const Color backgroundColor = Color(0xFFFAFAFA); // Hijau sangat pudar
  static const Color cardBackground = Colors.white;

  // Warna Teks
  static const Color textPrimary = Color(0xFF212121); // Hitam untuk teks utama
  static const Color textSecondary = Color(
    0xFF757575,
  ); // Abu-abu untuk teks sekunder
  static const Color textOnPrimary =
      Colors.white; // Putih untuk teks di atas warna hijau

  // Warna Status Order
  static const Color statusPending = Color(
    0xFFFFA726,
  ); // Kuning/Orange - Diproses
  static const Color statusShipped = Color(0xFF42A5F5); // Biru - Dikirim
  static const Color statusDelivered = Color(0xFF66BB6A); // Hijau - Sampai
  static const Color statusCancelled = Color(0xFFEF5350); // Merah - Dibatalkan

  // Warna Aksen Lainnya
  static const Color warning = Color(0xFFFF9800); // Orange
  static const Color error = Color(0xFFD32F2F); // Merah
  static const Color success = Color(0xFF43A047); // Hijau sukses
  static const Color star = Color(0xFFFFC107); // Kuning untuk rating

  // Warna Border & Divider
  static const Color borderColor = Color(0xFFE0E0E0);
  static const Color dividerColor = Color(0xFFBDBDBD);

  // warna umum
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color grey = Colors.grey;

  static const Color gray50 = Color(0xFFF9FAFB);
  static const Color gray100 = Color(0xFFF3F4F6);
  static const Color gray200 = Color(0xFFE5E7EB);
  static const Color gray300 = Color(0xFFD1D5DB);
  static const Color gray400 = Color(0xFF9CA3AF);
  static const Color gray500 = Color(0xFF6B7280);
  static const Color gray600 = Color(0xFF4B5563);
  static const Color gray700 = Color(0xFF374151);
  static const Color gray800 = Color(0xFF1F2937);
  static const Color gray900 = Color(0xFF111827);

  static const Color shimer = Color(0xFFF3F4F6);
}
